# Model weights are never committed. tgo downloads them into a cache.
/models/
/testdata/weights/
*.safetensors
*.gguf

cover.out
/dist/

# Built by `go build` inside the command directory.
/cmd/tgo/tgo
